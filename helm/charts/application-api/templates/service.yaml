apiVersion: v1
kind: Service
metadata:
  name: {{ include "application-api.fullname" . }}
  namespace: {{ .Release.namespace }}
  labels:
    {{- include "application-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "application-api.selectorLabels" . | nindent 4 }}
